include: package:lints/recommended.yaml

analyzer:
  exclude:
    - lib/src/generated/**
    # These sources are copied into a generated Flutter consumer application,
    # where Flutter SDK dependencies are available and analyzed separately.
    - tool/flutter_smoke/**
